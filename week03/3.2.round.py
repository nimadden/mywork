# program to round a float to an integer
# author Niall Madden

NumberToRound = float(input("Enter float number"))
RoundedNumber = round(NumberToRound)

print("{} rounded is {}".format(NumberToRound, RoundedNumber))